# @unloop/core

Pure TypeScript domain package for Unloop (no React Native / Expo).

```bash
# from repo root
npm test
npm run typecheck -w @unloop/core
npm run check-boundaries
```

Exports Session FSM, Challenge/Capability contracts, ports, policy/challenge selection, and a typed event emitter.
