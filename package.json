{
  "name": "unloop-yourself",
  "private": true,
  "version": "0.0.0",
  "description": "Unloop — privacy-first doomscroll interruption (monorepo)",
  "license": "AGPL-3.0-only",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "npm run test -w @unloop/core",
    "typecheck": "npm run typecheck -w @unloop/core && npm run typecheck -w @unloop/mobile",
    "check-boundaries": "scripts/check-boundaries.sh",
    "mobile": "npm run start -w @unloop/mobile"
  },
  "engines": {
    "node": ">=20"
  }
}
